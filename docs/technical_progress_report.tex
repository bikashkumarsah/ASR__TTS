\documentclass[11pt,a4paper]{article}
\usepackage[margin=1in]{geometry}
\usepackage[T1]{fontenc}
\usepackage{lmodern}
\usepackage{booktabs}
\usepackage{longtable}
\usepackage{array}
\usepackage{xcolor}
\usepackage{hyperref}
\usepackage{enumitem}
\hypersetup{colorlinks=true,linkcolor=blue,urlcolor=blue,pdftitle={Technical Progress Report}}
\newcommand{\project}{Nepali ASR/TTS: Syllable Tokenizer and Balanced Corpus Builder}
\newcommand{\status}[1]{\textcolor{blue}{\textbf{#1}}}
\begin{document}
\begin{center}
{\LARGE\bfseries Technical Progress Report\par}
\vspace{0.35em}
{\large \project\par}
\vspace{0.5em}
\textbf{Report date:} August 7, 2026 \qquad
\textbf{Overall status:} \status{final 50k corpus complete; source coverage and metadata balance validated}
\end{center}
\section{Purpose}
This report records the technical progress of the Nepali ASR corpus-building workflow.
It is designed for end-of-day updates so that decisions, validation evidence, risks, and next steps remain traceable.
\section{Current System Snapshot}
\begin{longtable}{>{\raggedright\arraybackslash}p{0.28\textwidth}>{\raggedright\arraybackslash}p{0.64\textwidth}}
\toprule
\textbf{Area} & \textbf{Current state} \\
\midrule
Final delivery corpus & 50,000 selected Nepali ASR sentences, curated from the annotated cloud pool without changing the incremental corpus state. \\
Primary inputs & Nepali-Text-Corpus Parquet shards, optional \texttt{compiled.txt}, and optional \texttt{Source\_book.txt}. \\
Metadata & Tense, polarity, gender, and sector labels are assigned through configurable rule-based annotation. \\
Selection & Metadata-stratified, syllable-aware greedy selection; prior selected IDs are excluded through \texttt{corpus\_state.json}. \\
Final artifact & \texttt{final\_50k\_all\_syllables.jsonl}; 50,000 JSONL records (approximately 81 MB). \\
Final corpus metrics & 1,168 covered syllables, 3,154,230 syllable tokens, and CV 3.4353. \\
Attainable source target & 1,168 syllables found in all 1,056 annotated pool chunks (51,431,998 candidate records); final source coverage is 1,168/1,168 (100\%). \\
Statistics source & Supplied final JSONL, final coverage report, and cloud-run output. \\
\bottomrule
\end{longtable}
\section{Workflow Architecture}
\begin{enumerate}[leftmargin=1.7em]
\item \textbf{Extract:} Stream source files, split text into sentences, filter quality, deduplicate normalized text, and write chunked JSONL pool files.
\item \textbf{Annotate:} Add tense, polarity, gender, and sector labels to every pool chunk.
\item \textbf{Select:} Stream annotated chunks, retain a bounded candidate reservoir for each metadata cell, and select the next balanced batch.
\item \textbf{Analyze:} Write a batch report with metadata distributions and syllable statistics.
\item \textbf{Merge:} Deduplicate selected batch records and write the final corpus JSONL file.
\end{enumerate}
\section{Completed Work}
\subsection{Corpus and Distribution Status}
The 150k incremental run completed through Batch 30. The subsequent source-aware inventory scanned all 1,056 annotated pool chunks and established 1,168, rather than 1,182, as the final attainable target. The difference is expected: the latter count came from a broader raw-text scan, while the pool inventory reflects the filtered and annotated records available to the selector.
The final curator produced exactly 50,000 records. It covers all 1,168/1,168 annotated-pool syllables (100\% attainable-source coverage), 3,154,230 syllable tokens, and CV 3.4353. It also covers 1,168/1,782 lookup tokens (65.54\%). The remaining lookup tokens are not available in the final candidate pool and are therefore not selection failures.
\begin{longtable}{lrr}
\toprule
\textbf{Metadata axis} & \textbf{Label} & \textbf{Count} \\
\midrule
\endhead
Tense & Future & 12,439 \\
& Mixed & 12,516 \\
& Past & 12,525 \\
& Present & 12,520 \\
\addlinespace
Polarity & Negative & 16,688 \\
& Neutral & 16,695 \\
& Positive & 16,617 \\
\addlinespace
Gender & Feminine & 16,689 \\
& Masculine & 16,608 \\
& Neutral & 16,703 \\
\addlinespace
Sector & Business & 5,567 \\
& Conversational & 5,564 \\
& Education & 5,561 \\
& Entertainment & 5,566 \\
& Formal & 5,565 \\
& Health & 5,565 \\
& News & 5,566 \\
& Sports & 5,567 \\
& Technology & 5,479 \\
\bottomrule
\end{longtable}
Final marginal metadata balance passed the two-percentage-point acceptance guard by a wide margin. The largest absolute deviation from uniform was 0.153 percentage points (technology sector); tense, polarity, and gender maxima were 0.122, 0.099, and 0.117 percentage points respectively. The final sentence length range is 12--80 syllables, with median 66 and mean 63.1.
\subsection{Memory-Safety Improvement}
\textbf{Problem.} The original selection path read every \texttt{pool\_chunk\_*.jsonl} file into one Python list.
On a large cloud run this attempted to materialize all 2,031 pool chunks and created an unnecessary RAM demand.
\textbf{Implemented solution.} Selection now streams every pool chunk and uses reservoir sampling to retain only a bounded number of candidates per metadata cell.
The default is 2,000 candidates per cell, configured with \texttt{--max-candidates-per-cell}; the complete pool is no longer held in memory during selection. The observed cloud run used at most about 10 GB of a 120 GB instance, leaving room to increase this bounded cap for wider general candidate choice.
In multi-worker mode this is a global per-cell cap: the allowance is divided between workers and chunk files are interleaved, preventing the cap from multiplying by the worker count.
For rare-syllable recovery, the pipeline now also builds a distinct bounded index of candidates per requested source syllable. This avoids relying on random cell reservoirs to discover rare targets and uses only 256 retained examples per target by default.
\subsection{Cloud CPU Scaling Improvement}
The pipeline accepts \texttt{--workers} and defaults to all CPU cores visible to Python.
The recommended cloud invocation is:
\begin{verbatim}
CLOUD_WORKERS="$(nproc)"
python -m dataset_builder.pipeline run-batch \
  --batch-id 1 --target-size 5000 --max-corpus 0 \
  --max-chunks 0 --workers "$CLOUD_WORKERS"
\end{verbatim}
Parallel Parquet extraction, hash-partitioned deduplication, chunk writing, annotation, streaming, and selection use this worker allocation.
The previous 32-worker cap in the extraction merge phase was removed. A one-worker path is available for low-memory or interactive-notebook execution.
\subsection{Workflow Reliability Improvement}
Partial annotation can bias metadata selection. The pipeline now checks pool chunks without materializing the pool and stops with a clear error if annotation is incomplete.
Annotation processes sequentially when one worker is requested, avoiding a macOS/interpreter process-spawn issue in interactive sessions.
\section{Validation Evidence}
\begin{longtable}{>{\raggedright\arraybackslash}p{0.30\textwidth}>{\raggedright\arraybackslash}p{0.58\textwidth}}
\toprule
\textbf{Check} & \textbf{Result} \\
\midrule
Python syntax compilation & Passed for all scripts under \texttt{scripts/}. \\
2,031-chunk memory fixture & Passed with a cap of 25 candidates per metadata cell; selected 10 unique records without loading the entire pool. \\
Multi-worker selection fixture & Passed with 4 workers over 2,031 chunks; the merged reservoir held 25 candidates and selected 10 unique records. \\
Cloud production run & Completed: 51,431,998 candidates were extracted into 1,056 chunks, annotated, and reused for incremental selection and final curation. \\
Source coverage inventory & Completed with 32 workers: 1,168 attainable syllables were found across all annotated pool chunks. \\
Final 50k curation & Passed: 50,000 records were written; all 1,168 source targets were covered; source coverage was 100\%; CV was 3.4353. \\
Final metadata validation & Passed: the maximum marginal deviation was 0.153 percentage points, below the 2.0-point acceptance limit. \\
End-to-end fixture & Extract, annotate, streaming selection, report generation, and state writing completed successfully. \\
Parallel Parquet fixture & Passed with 3 accepted records and 3 unique output IDs. \\
Existing corpus consistency & Passed at 50k before expansion: 10 batches, 50,000 selected records, 50,000 unique IDs, and 50,000 merged records. \\
\bottomrule
\end{longtable}
\section{Known Constraints and Operating Guidance}
\begin{itemize}[leftmargin=1.7em]
\item The full source corpus is large. Download only the final corpus, cumulative state, and reports unless a resume is required.
\item Full-core operation increases throughput, while each worker keeps a bounded reservoir. The cap is global rather than per worker; increase \texttt{--max-candidates-per-cell} to widen coverage choice, or reduce it/use \texttt{--workers 1} if RAM is constrained.
\item Distribution reports describe the selected corpus; they do not replace qualitative ASR-data review or audio/text alignment validation.
\item Retain the cloud pool if a future refresh is needed. Re-extraction and re-annotation are unnecessary for the completed final curation.
\item Per-batch coverage is not aggregate coverage. The completed final corpus establishes 1,168/1,168 (100\%) coverage against the annotated-pool inventory.
\item The observed 10 GB peak at a 2,000-candidate cap did not require allocating all 120 GB. The completed final build used an 8,000-candidate per-cell reservoir plus 256 retained examples per source syllable.
\item The lookup vocabulary and candidate-pool inventory are different measures. The 1,168-syllable inventory is the final-coverage target; lookup syllables missing from that inventory are not reachable by selection.
\item If a batch fails after a prior batch completes, do not reset the corpus. Pull the current code and rerun only the failed batch; the state file preserves previous selections.
\end{itemize}
\section{Final Curation Outcome and Delivery Guidance}
The planned strict recovery and final curation are complete. The candidate-pool inventory showed that all 1,168 attainable targets were already represented in the cloud state before Batch 31; Batch 31 therefore performed ordinary balancing. The final curator then started from a fresh state, seeded coverage with 410 records, and selected the remaining 49,590 records under the metadata and syllable-balance objectives.
\begin{enumerate}[leftmargin=1.7em]
\item \textbf{Deliver the corpus.} Use \texttt{final\_50k\_all\_syllables.jsonl} as the sole final dataset. It contains exactly 50,000 records and is approximately 81 MB.
\item \textbf{Retain the evidence.} Keep \texttt{final\_50k\_all\_syllables\_report.json} with the dataset. It records 100\% source-target coverage, the syllable CV, sentence-length statistics, and metadata distributions.
\item \textbf{Keep the source inventory.} Retain \texttt{source\_syllable\_inventory.json} with the cloud artifacts. It establishes which lookup tokens are actually attainable from this candidate pool.
\item \textbf{Optional follow-up.} Before using the text corpus in an ASR training pipeline, perform qualitative text review and validate audio/text alignment once recordings are available.
\end{enumerate}
The source inventory, strict recovery index, and non-destructive final curator are implemented. The final delivery is complete; the incremental 150k-plus corpus remains available only as a broader control dataset.
\section{Daily Work Log}
\begin{longtable}{>{\raggedright\arraybackslash}p{0.15\textwidth}>{\raggedright\arraybackslash}p{0.22\textwidth}>{\raggedright\arraybackslash}p{0.51\textwidth}}
\toprule
\textbf{Date} & \textbf{Status} & \textbf{Progress and evidence} \\
\midrule
\endhead
2026-08-05 & Completed & Updated the README and pipeline instructions; replaced full-pool selection loading with bounded streaming reservoirs; enabled full cloud-core scaling; validated single-worker and multi-worker fixtures; confirmed the existing 50k corpus consistency; pushed commit \texttt{41637d2}. A 32-worker cloud run completed Batch 1 from 51,431,998 candidates (1,056 chunks) and selected 5,000 balanced records. Batch 2 exposed a per-worker reservoir memory multiplication; the global-cap fix was implemented and validated locally. \\
\addlinespace
2026-08-06 & Completed & The cloud coverage expansion completed batches 11--30 using the existing extracted and annotated pool. The corpus reached 150,000 selected records and established the broad control corpus used for final curation. A source-aware inventory, bounded rare-syllable candidate index, strict Batch 31 recovery path, and non-destructive final 50k curator were implemented and exercised with an isolated coverage fixture. \\
\addlinespace
2026-08-07 & Completed & The 32-worker cloud inventory scanned 1,056 annotated chunks and found 1,168 attainable syllables across 51,431,998 candidates. All were already present in the cloud state before Batch 31. The final curator produced \texttt{final\_50k\_all\_syllables.jsonl}: 50,000 records, 3,154,230 syllable tokens, 1,168/1,168 source-target coverage (100\%), lookup coverage 65.54\%, and CV 3.4353. Metadata balance passed with a maximum marginal deviation of 0.153 percentage points, below the 2.0-point limit. \\
\addlinespace
\rule{0pt}{3.6em}\textit{YYYY-MM-DD} & \textit{Planned, active, or complete} & \textit{State the goal, files or commands changed, measured result, validation performed, blockers, and the next action.} \\
\addlinespace
\rule{0pt}{3.6em}\textit{YYYY-MM-DD} & \textit{Planned, active, or complete} & \textit{State the goal, files or commands changed, measured result, validation performed, blockers, and the next action.} \\
\bottomrule
\end{longtable}
\section{End-of-Day Update Procedure}
At the end of each workday, provide the day's commands, outputs, errors, decisions, and completed tasks. Then request:
\begin{quote}
\textit{Update \texttt{docs/technical\_progress\_report.tex} for today's work. Include completed work, measured results, validation, blockers, next steps, and any changes to the corpus or distribution statistics. Compile the PDF only when explicitly requested.}
\end{quote}
The update should add a Daily Work Log row and revise the relevant technical sections whenever the design, validation evidence, risks, or results change.
\end{document}
